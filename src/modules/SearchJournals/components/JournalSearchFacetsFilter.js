import React from 'react';

export const JournalSearchFacetsFilter = () => {
    return `JournalSearchFacetsFilter: ${Math.floor(Math.random() * 100)}`;
};

export default React.memo(JournalSearchFacetsFilter);
